<%= form_for(@walkr) do |f| %>
  <% if @walkr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@walkr.errors.count, "error") %> prohibited this walkr from being saved:</h2>

      <ul>
      <% @walkr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <!-- <div class="field">
    <%= f.label :rating %><br>
    <%= f.text_field :rating %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
